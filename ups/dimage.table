File=Table
Product=dimage

Group:
Flavor=ANY
Qualifiers=""

Common:
   Action=setup
      proddir()
      setupenv()
      envSet(GOOGLE_DIR, /mount/hercules1/sdss/dr7sky)
      pathAppend(PATH, ${UPS_PROD_DIR}/bin)
      pathAppend(LD_LIBRARY_PATH, ${UPS_PROD_DIR}/lib)
      pathAppend(PYTHONPATH, ${UPS_PROD_DIR}/bin)
      envPrepend(IDL_PATH, +${UPS_PROD_DIR}/pro)
End:

